{{>partials/header}}

    <span class="container cep-query-result">
        <figure class="container figure-locale">
            <img src="./img/icon-for-address-28 (cópia).png" width="50" alt="" srcset="">
        </figure>
        {{#address.erro}}


            <h2 class="title">Resultado da Busca</h2>
            <h4>Não foi possível encontrar o endereço com o cep digitado :( </h4>
            
        {{/address.erro}}
        {{^address.erro}}
            {{#address}}
                <h2 class="title">Resultado da Busca</h2>
                <table class="flat-table">
                    <thead><th>Logradouro</th><th>Bairro</th><th>Cidade</th><th>UF</th><th>CEP</th></thead>
                    <tbody>
                        <tr>
                            <td>{{logradouro}}</td>
                            <td>{{bairro}}</td>
                            <td>{{localidade}}</td>
                            <td>{{uf}}</td>
                            <td>{{cep}}</td>
                        </tr>
                    </tbody>
                </table>
            {{/address}}
        {{/address.erro}}
    </span>


            <h1 class="title">CONSULTA CEP</h1>
        <form method="GET" action="cep" class="form-cep-query">
            <input type="text" size=8 maxlength=8 name="cep" id="" placeholder="Digite o nº do CEP" value={{cep}} required>
            <button type="submit" >Enviar</button>
        </form>

        